# Production Environment Variables for Render
# Add these as Environment Variables in Render Dashboard (NOT as a .env file)

# Server Configuration
PORT=5000
NODE_ENV=production

# Supabase Configuration (REQUIRED)
SUPABASE_URL=https://yjynnclmzueqxitqszdt.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# CORS Configuration (Update after deploying frontend to Vercel)
# Format: comma-separated URLs (no spaces after commas)
ALLOWED_ORIGINS=https://your-vercel-app.vercel.app

# Optional: Database Configuration (if using direct PostgreSQL connection)
# DATABASE_URL=postgresql://postgres:password@host:5432/postgres
